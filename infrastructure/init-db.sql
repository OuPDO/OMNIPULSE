-- OMNIPULSE PostgreSQL Init Script
-- Creates the platform database if it doesn't exist

-- Enable useful extensions
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pgcrypto";
