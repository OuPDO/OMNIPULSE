{
  "name": "@omnipulse/backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "php artisan serve --host=0.0.0.0 --port=8000",
    "test": "php artisan test",
    "lint": "echo 'PHP linting via PSR-12'",
    "build": "echo 'No build step for Laravel'"
  }
}
